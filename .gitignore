# 玩家存档 (各自本地, 不入库)
saves/*
!saves/.gitkeep

# 机密 (绝不入库): 环境变量 / 各类密钥
.env
*.key
secrets.*

# python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# 可再生成的原始数据缓存 (build_ocean_data.py 会自动重建, 鱼糕js需手动放置)
data/raw/
